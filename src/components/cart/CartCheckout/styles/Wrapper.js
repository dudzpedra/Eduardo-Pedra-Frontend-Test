import styled from "styled-components";

export const CheckoutWrapper = styled.div`
  border-top: 1px solid rgba(229, 229, 229, 1);
  padding-bottom: 1rem;
  width: 15vw;
`

CheckoutWrapper.displayName = 'CheckoutWrapper'